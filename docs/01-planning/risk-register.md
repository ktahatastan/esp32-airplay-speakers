---
status: active
owner: orchestrator
updated: 2026-09-13
tags: [risk, fmea]
---

# Risk kaydı

| Risk | Etki | Olasılık | Azaltma | Sahip |
|---|---|---|---|---|
| Hoparlör ile telefon aynı L2 ağında değilse keşif ve saat sessizce çalışmaz | Yüksek | Yüksek | Bugün gerçekten yaşandı: kart yönlendiricinin misafir SSID'sindeyken ana ağdaki Mac kartı `dns-sd` ile hiç görmedi ve ARP girdisi `incomplete` kaldı, aynı Mac aynı subnet'te sekiz komşuyu çözerken. Genel istemci yalıtımı değil, misafir ağının tasarımı. mDNS keşfi ve PTP saati çoklu yayındır, yalıtılmış misafir/IoT ağını aşmazlar; bu firmware'de düzeltilebilecek bir kusur değil, ürünün kurulum koşulu. Cihaz durumu kendi tarafından ayırt edemiyor (birleşme ve mDNS başarılı görünür), o yüzden azaltma kurulum yönergesinde ve portal sayfasının ağ listesi metnindedir; hoparlörün telefonla aynı SSID'de olması AirPlay keşfinin önkoşulu. Tanı yordamı [[../06-testing/devkit-bring-up\|geliştirme kartı kaydında]] | firmware |
| FairPlay yanıtları sabit kodlu; iOS/macOS güncellemesi eşleşmeyi kırabilir | Yüksek | Orta | Sürüm sabitleme, vendor edilmiş kopya, kırılırsa AirPlay 1 yoluna geçiş | firmware |
| Seçilen yığının lisansı ticari olmayan kullanımla sınırlı | Orta | Kesin | Kabul edildi ve ADR-0007'ye yazıldı; release asset'leri lisans metnini taşır | orchestrator |
| Sürücü empedans eğrisi ve `Fs` bilinmiyor; DC dirençler ölçüldü (woofer 4,0 Ω, tweeter 3,5 Ω) ama crossover köşesi hâlâ tahmin | Kritik | Yüksek | G0 empedans süpürmesi ve düşük seviyeli G2 — önce tek woofer ve tek tweeter | acoustics |
| Tweeter'a DC/düşük frekans gider | Kritik | Orta | HPF, limiter, mute | hardware |
| Susturma hatları firmware'de sürülüyor ama şemada hiç çizilmemişti | Kritik | Kesin | **Çizim tarafı kapatıldı (2026-09-08), fiziksel taraf açık.** `hk_audio_hw.c` `GPIO21`/`GPIO13`'ü açılıştan itibaren susturulmuş tutup sıralı bırakıyordu; oysa SVG paftasında iki pin de uçsuz etiketti, PCM5102A blokunda `XSMT` pini hiç yoktu, sayfa "XSMT=HIGH / kart varsayılanı" diyerek kabul edilmiş ADR-0011 ile çelişiyordu ve KiCad üreteci iki neti `EXPECTED_OPEN_NETS`'te tutarak yokluklarını **onaylıyordu**. Yani susturma katmanı hiçbir şeyi sürmüyor olabilirdi ve hiçbir denetim bunu söylemezdi. Şimdi pull-down referanslı bir parça olarak çizildi — `XSMT`'de `R6` (ADR-0011) — `XSMT` netine prob noktası eklendi, BOM ve kablolama planı hizalandı. Amfi tarafı 2026-09-12'de kapandı: XH-A232'de susturma girişi **yok** (sahibin tespiti; güç girişi, ses girişi ve hoparlör çıkışları dışında bağlantı yok), `GPIO21` rezervasyonu kaldırıldı ve zincirdeki tek susturma DAC `XSMT`'dir. Kalan risk fiziksel ve üç ölçümdür: (1) PCM5102A modülünde `XSMT` pad'i 3,3 V'a sert bağlıysa pull-down bölücüye döner ve `GPIO13` LOW sürerken rayı zorlar; (2) amfilerin kendi açılış/kapanış pop'u firmware'in erişemediği bir şeydir ve adaptör takılıp çekilirken DAC susturuluyken ne ürettiği kaydedilmedi; (3) hiçbir kartta `TP33` açılışta ölçülmedi. Üçü kaydedilmeden "pop yok" veya susturma `PASS` denemez ve empedansı ölçülmemiş sürücülere sinyal verilmez (kablolama planının PCM5102A modül ayarları, kanal kuralları ve güç açma/kapatma kaydı bölümleri). 2026-09-12'de DSP zinciri ürünün çıkış arka ucu oldu (ADR-0022): sürücünün önündeki tek yazılım katmanı o zincir ve DAC'ın `XSMT`'sidir, amfide susturma yoktur, dolayısıyla "pop yok" bir kabul ölçütü olarak kayıttan çekildi — geçiş kaydedilir ve kararlaştırılmış seviyeye göre yargılanır. Veri sayfaları okundu (SLOS528F, SLAS859C, MP1584): TPA3110D2'nin pop tarifi `SD` ister ve kart onu çıkarmaz, sayısal UVLO eşiği yayımlanmamış (8 V önerilen asgari), açılış 14 ms / kapanış 2 µs; PCM5102A çıkışı toprak merkezli, 104 örneklik yumuşak rampa, kendi harici güç algılama `XSMT` modu var; MP1584 4,5 V'a kadar çalışır. Çekmede amfilerin DAC'tan önce düşmesi beklenir ama kanıtlanmamıştır. Adaylar (VIN algılama bölücüsü, yüksek taraf anahtar, `SD` pinine lehim, röle) kablolama planı §3.4'te karar değil aday olarak, ikisi `G1` zorlamadıkça reddedilmiş olarak duruyor; seçim `G1` kaydından sonra ve ADR ile | hardware |
| Buck/LED/Wi-Fi ses gürültüsü | Yüksek | Orta | DAC'ın kendi buck'ı (B) ESP32'ninkinden (A) ayrı: paylaşılan tek buck tezgâhta DAC'a duyulur hışırtı verdi (sahibin 2026-09-12 gözlemi, düzenek ayrıntısı kaydedilmedi; ADR-0020). Yerleşim, yıldız toprak, filtre; iki buck'ın da gürültüsü G1'de ölçülür | hardware |
| Ters polariteli veya yanlış gerilimli adaptör girişi | Yüksek | Düşük | Merkez-artı 5,5×2,1 jak; G1'de ilk güç vermeden önce jak polaritesi ölçülür; adaptörün boşta çıkışı bağlanmadan önce ölçülür ve 25,5 V'un altında olmalıdır (24 V, amfinin 26 V sınırının 2 V altında); `VIN` üzerinde seri Schottky/ideal diyot adayı — 2,9 A'da yaklaşık 1 W veya üstü ısınır, karar G1'de (ADR-0020) | hardware |
| Dört amfi birlikte sürülünce sekiz BTL kanal 70 W'ı aşar; `VIN` çöker ve ESP32-S3 şarkı ortasında sıfırlanır | Yüksek | Orta | Bütçe 2026-09-12'den beri tepe tavanlarına katlanmıyor — tepe tavanları `G2`'nin sürücü koruma sayılarıdır, bütçenin altında kalır ve onu taşımaz; DSP'de bütçenin kendi katı var: `hk_supply_limiter`, iki dalın toplamı üzerinde ortalama, tepe limiter'lardan önce ortak kazanç (ADR-0022). İki sayısı (`supply_budget_sq`, `supply_window_ms`) hiç varsayılmaz ve G1 S7'de dört amfi **4 Ω sınıfı** dummy-load'a sürülürken adaptörün 2,9 A noktasında ölçülür — `VIN` çökmesi, brownout ve DSP'nin log'a bastığı dedektör okuması (ADR-0020); 8 Ω'da alınmış bir geçiş gerçek akımın yarısıdır ve geçmez. Tezgâh yer tutucusu (1,0) bir tam ölçekli dalın ortalama-karesidir — doğrulayıcı sınırının (2,0) yarısı — ve tezgâh kazançlarıyla (0,25 / 0,18, en çok ≈ 0,095) ancak kullanıcı EQ'su yükseltirse devreye girer; açılışta bunu söyler. İlk enerjilenen yol tek amfi, tek woofer, tek tweeter'dır; diğer üç amfi ancak o çift G0-G2'yi geçince sürücülere bağlanır | hardware |
| TPA3110D2 24 V'ta 4 Ω sınıfı yükle karakterize edilmemiş: veri sayfasının Mutlak Azami Değerleri (SLOS528F §7.1) BTL için asgari yükü `PVCC > 15 V`'ta 4,8 Ω verir, 4 Ω eğrileri 16 V'ta biter, 24 V / 4 Ω için yayımlanmış çıkış gücü yok; iki Nova sürücüsü de 4 Ω sınıfı (`Re` 4,0 / 3,5) ve ürün beslemesi 24 V (ADR-0020) | Kritik | Yüksek | Bu bir veri sayfası okumasıdır, ölçüm ya da karar değil; ADR-0020'nin 24 V seçimi hakkında bir hüküm de değil, o ADR'nin sahibine açık bir soru. **Tıkadığı şey:** `G0` iki sürücünün `Z_min`'ini verene ve `G1` D/S1 kademesi 24 V'ta 4 Ω sınıfı non-inductive yüke amfinin termal ve koruma (kısa devre / DC algılama kilidi) davranışını kaydedene kadar **hiçbir sürücü 24 V'ta bağlanmaz**. Seçenekler o kayıttan sonra ADR ile: 24 V'ta kal (ölçülmüş davranışla), beslemeyi ≤ 15 V'a indir (headroom yarılanır, ADR-0020'nin gerekçesi değişir), `PLIMIT` ile sanal ray (kartın `PLIMIT` bağlantısı bilinmiyor). Kablolama planı §7.4 D/G ve §9; sürücü ölçüm planı "Bu neyi açıyor" | hardware |
| Tepe tavanı ölçekleme modeli sabit kazançlı amfiye uymuyor: `hk_profile_ceiling_at()`'in `referans/besleme` çarpanı "sürücüdeki volt tezgâhtakiyle aynı kalır" varsayımıyla yazılmıştı; TPA3110D2'de çıkış ray kırpana kadar kazanç × giriştir (SLOS528F Tablo 3), beslemeyle ölçeklenmez | Orta | Kesin | 2026-09-12'de iddia kayıttan çekildi ve çarpan tek yönlü yapıldı, `min(1, referans/besleme)`: referansın altındaki besleme tavanı yükseltmez (12 V'a yapılandırılmış bir yapıda 24 V tavanının iki katına çıkması tek tehlikeli yöndü). Aşağı yön dijital tavanı yarıya indirir; tezgâh 12 V rayını kırpmadıysa bu sürücüdeki voltu da yarıya indirir, kırptıysa 24 V rayı daha geç kırpar ve yarıya inen tavan sürücüye tezgâhın duyduğundan fazlasını verebilir — hangisinin geçerli olduğunu `C3` (amfinin kazanç strap'i) söyler. Kalan risk sıradadır, formülde değil: tezgâh seviyesi, firmware'in notuna göre, 12 V'ta ray kırpmasıyla sınırlıydı ve 24 V'ta aynı kaydırıcıda sürücü iki katına kadar gerilim görebilir — `C3` okunmadan 24 V'ta sürücüde dinleme yok; tezgâh yapısı ürün beslemesinde (`CONFIG_HK_SUPPLY_MV` = 24000, tezgâh referansı 12000) derlendiğinde açılışta bunu ayrıca söyler. Geçici kural: `G2` tavanları kullanılacakları beslemede (`reference_supply_mv` = 24000) kaydeder, çarpan üründe tam 1 olur. Modelin kendisi (DAC'a göre tavan + ray kırpma reddi + `amp_gain_db`) `C3` sayısı gelince ADR-0020'ye ek ADR ile karara bağlanır; şema 2 `amp_gain_db`'yi taşımak için alanı şimdiden açtı | firmware |
| OTA sırasında brick | Yüksek | Orta | A/B OTA, G6 | firmware |
| Provisioning kimlik bilgisi cihaza yazılmadı | Yüksek | Kesin | **Konu kapandı (2026-09-13, [[../07-decisions/ADR-0023-pinless-provisioning\|ADR-0023]]).** Satır 2026-08-31'de açıldı: üretim aracı cihaz başına Security 2 kimlik bilgisi üretiyor, firmware kimlik bilgisi yoksa provisioning'i açmayı reddediyordu; 2026-09-08'de ürün kartına yazıldı ve yüklendiği ölçüldü. ADR-0023 ile firmware'in kurulum için ihtiyaç duyduğu bir kimlik bilgisi kalmadı: boş `factory_cal`'lı bir kart da provisioning açar, reddetme özelliği bilerek kaldırıldı. Ses izni bundan bağımsızdır ve `schema` + `profile` + geçerli karar ister (ADR-0022). Daha önce kimlik bilgisi yazılmış kartlarda (ürün kartı, geliştirme kartı) eski satırlar `factory_cal`'da durur, firmware onları hiç okumaz; yeniden flaşlama gerekmez | firmware |
| Uygulamasız kurulum yolu yazıldı ama hiç denenmedi | Yüksek | Orta | 2026-09-05'te bu satır "yol yok" diyordu: `wifi_prov_scheme_softap` sayfa sunmuyor, o ağa katılan kullanıcının önüne hiçbir şey açılmıyordu. [[../07-decisions/ADR-0015-softap-captive-portal\|ADR-0015]] karşılığını yazdı: portal `hk_portal` bileşeninde, kimlik bilgileri `wifi_prov_mgr_configure_sta()` ile yöneticiye veriliyor; kurulum ağı o gün WPA2'ydi, [[../07-decisions/ADR-0023-pinless-provisioning\|ADR-0023]] ile **açık** (`PROV_Merzarkabul-XXXX`, kilit yok) ve sayfa ağın açık olduğunu bir cümleyle söylüyor. Kalan risk artık "yok" değil, **doğrulanmadı**: profiller derleniyor ve form ayrıştırıcısı host'ta testli, ama hiçbir telefon bu sayfayı açmadı — ne WPA2'li hâlini, ne açık hâlini. Captive portal algılamasının iOS ve Android'de gerçekten tetiklendiği ve kilitsiz ağın listede göründüğü ölçülene kadar PRD-004 kapanmaz. Uygulamalı yol da yeniden açık: "ESP BLE Provisioning" 2026-09-05'te iPhone'da Security 2 ile uçtan uca çalıştı, ADR-0023'ün Security 1 / `no_pop` akışı hiçbir telefonda denenmedi; Espressif'in "ESP SoftAP Prov" uygulaması 2026-09-03'te Security 2 el sıkışması geçtikten sonra AES-GCM katmanında düşüyordu (`mbedtls_gcm_auth_decrypt : -18`) ve o hata Security 1'in AES-CTR katmanı hakkında bir şey söylemez — denenmedi | firmware |
| Kurulum penceresi PIN'siz ve kurulum ağı açık (ADR-0023) | Yüksek | Düşük | **Kabul edildi**, sahibin sözüyle: *"evde kullanacağım sorun yok kolay olsun kurulumu."* Kabul edilen, adıyla: (1) pencere açıkken menzildeki herkes hoparlörü kendi ağına kurabilir — iki taşımada da sahiplik kanıtı yok; bunu yapan ev parolasını öğrenmez, sahibi 5 sn basışla sıfırlayıp yeniden kurar. (2) Pasif dinleyici uygulama yolundan bir şey öğrenmez (X25519 + AES-CTR). (3) Pencere sırasında menzildeki aktif bir saldırgan — sahte bir `PROV_Merzarkabul-XXXX` cihazı ya da ağı — oturumun ortasına girebilir, çünkü hiçbir şey sahipliği kanıtlamaz; Security 2'nin cihaz kanıtı bunu engelliyordu. (4) Uygulamasız portal formu açık ağda düz HTTP'dir: orada yazılan ev parolası pencere sırasında menzildeki bir dinleyici tarafından okunabilir; BLE/uygulama yolunda bu maruziyet yoktur. Pencere ilk açılışta kurulana kadar açıktır, sonrasında yalnız buton basışıyla 10 dakika. Kabulün sınırı: hoparlör bir evde durur; ortak alan, ofis ya da konuk ağı kabulün dışındadır. Azaltma bilerek yok — daha güçlü bir mod yalnız ADR-0023'ü aşan bir ADR ile döner (`AGENTS.md` kilitli kararlar). Hiçbir telefonda denenmedi; deneme listesi `TODO.md` P5 | orchestrator |
| `hk_provision`'ın üç-başarısızlık geri dönüş dalı cihazda erişilemez | Orta | Kesin | Kayıt, politika değişikliği değil (ADR-0023). `hk_provision.c`'de üç ardışık başarısız katılımdan sonra sınırsız bir pencere açan dal (`HK_PROV_MAX_FAILURES`, `hk_provision.c:150-154`) host'ta testlidir, ama `hk_main` politikaya yalnız tick, buton kısa basış, ağ sıfırlama ve fabrika sıfırlama olaylarını verir; `CONNECT_OK` / `CONNECT_FAIL` / `CREDENTIALS` olaylarını hiçbir çağıran üretmez ve `hk_network` beş denemeden sonra hata bayrağıyla durur, `on_network_status` bunu politikaya çevirmez. Yani "üç başarısız katılımdan sonra butonsuz açılan pencere" bugün cihaz davranışı değildir — ne bir açık, ne bir güvence. PIN kalkınca bu ayrım önem kazandı: dal bağlanırsa pencere sınırlı mı sınırsız mı olacağı o gün kararlaştırılır; silinirse başlık ve host testi onunla gider. İkisi de ayrı bir iştir (`TODO.md` P5) | firmware |
| İlk imzalama anahtarı public depodayken ifşa oldu | Orta | Kesin | **Kapatıldı.** Depo `ktahatastan`'a taşındı ve private yapıldı; anahtar depodan kaldırıldı ve parmak izi `docs/credentials/burned-keys.txt`'e yazıldı. Yayın hattı o anahtarla imzalamayı kalıcı reddediyor — git geçmişinden geri getirilse bile, sınandı. Yerine çevrimdışı üretilmiş, depoya hiç girmemiş anahtar; açık yarısı sabitlenmiş, özel yarısı `release` ortam secret'ında. Sahada cihaz ve yayımlanmış sürüm olmadığı için ifşa penceresinde imzalanmış hiçbir şey yok | orchestrator |
| Canary/stable akışı hiç çalıştırılmadı | Orta | Kesin | Adresler, işaretçi sürümler ve `promote.yml` yazıldı; terfinin aynı ikiliyi taşıdığı yerelde ölçüldü (iki manifest arasında yalnız `channel` farklı). Ama hiç sürüm yayımlanmadı ve hiçbir cihaz güncellenmedi: `G6` matrisinin tamamı açık | firmware |
| AirPlay yığını vendor'lanmadı; F1 boyut kanıtı yeniden üretilemiyor | Yüksek | Kesin | **Kapatıldı** (2026-09-05). Yığın `firmware/components/hk_airplay/vendor/` altına `38027441ff43`'ten birebir kopyalandı ve entegrasyonun biçimi ADR-0013'te kayıtlı: vendor ağacında tek satır değişiklik yok, adaptör iki fonksiyon. Boyut kanıtı artık her CI koşusunda yeniden üretiliyor; geliştirme profilinde ölçülen imaj 1.642.880 bayt. Yukarı akış kaybolsa bile kopya depoda | firmware |
| İlk Wi-Fi birleşmesi aralıklı olarak düşüyor, sebebi kaydedilmedi | Orta | Orta | Geliştirme kartında üç açılışın ikisinde birleşme ~14,3 s'de düşüp ~20 s'de temiz kuruldu; yeniden bağlanma her seferinde toparladı, yani kullanıcıya görünen arıza yok. Kopma sebebi kodu kaydedilmedi. Aday açıklamalar (anten yerleşimi, WPA3-SAE anlaşması, AP band steering) ölçülmedi ve hiçbiri elenmedi. Ürün kartında tekrar bakılacak; o kartta da sürerse sebep kodu kaydedilip ayrı ele alınacak | firmware |
| Kimlik bilgisi yazmak cihazı "kalibre" gösteriyor; G0 kapısı yapısal olarak delik | Kritik | Kesin | **Kapatıldı** (2026-09-08). Ses izni artık şema sürümünün varlığından değil, `factory_cal` içinde bir **`profile` blob'unun bulunmasından** geliyor: `hk_storage_audio_permitted()` ikisini birden istiyor. Kimlik bilgisi üreticisi profil yazmadığı için provisioning artık kalibrasyon iddiası taşımıyor. Açılış raporu da netleştirildi — depo kapısının kararı ile ses yolunun nihai hükmü ayrı yazılıyor, çünkü tek satır "audio NOT permitted" derken alıcının I2S'i saatlemesi mümkündü. Tezgâhta ses yolunu çalıştırabilmek için istisna gizli değil, **açık**: `CONFIG_HK_BENCH_AUDIO_WITHOUT_PROFILE` yalnız `sdkconfig.bench` fragmanında, varsayılan kapalı, ve açıkken açılışta iki uyarı basıyor. Geçerlilik yarısı 2026-09-12'de kapandı (ADR-0022): `hk_main` açılışta blob'u `hk_profile_load()` ile — arka ucun kullandığı aynı yargıçla — okuyup zincire çeviriyor, kararı `hk_storage_profile_judged()` ile depoya veriyor ve `hk_storage_audio_permitted()` şema + varlık + geçerli karar üçünü birden istiyor. Var ama reddedilmiş bir profil artık DAC'ı sıfır yazan bir zincire açmıyor: açılış raporu `present:<verdict>` basıyor, ses susturulu kalıyor. Şema 1 blob'u şema 2'de adıyla reddedilir (açılış raporunda `present:schema`) — kayıt hiçbir cihaza yazılmadığını söylüyor, tezgâh kartında biri varsa güvenli tarafa düşer ama sürpriz olur. Tezgâh istisnası (`CONFIG_HK_BENCH_AUDIO_WITHOUT_PROFILE`) değişmedi ve hâlâ yalnız `sdkconfig.bench`'te; release hattı her `HK_BENCH_*` sembolünü reddeder | firmware |
| Paralel agent çakışması | Orta | Orta | Tek orkestratör ve tek yazma sahibi | orchestrator |
| Belge sapması: kanonik plan ile şema/BOM ayrışması | Yüksek | Orta | `scripts/check_docs.py` birleşme öncesi zorunlu; mimari değişiklik ADR'siz birleşmez | orchestrator |
