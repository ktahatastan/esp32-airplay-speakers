# Signing keys that must never sign a release again, by SHA-256 of their
# public half (espsecure.py extract_public_key, then shasum -a 256).
#
# The publish job in .github/workflows/release.yml checks HK_SIGNING_KEY
# against this list and refuses to sign if it matches. A key lands here when it
# has been exposed, and it never leaves: "probably fine now" is not a state a
# signing key can be in.
#
# Blank lines and # comments are ignored.

# 2026-08-31 — the first key, generated and committed to the repository
# (commit 9afd991). The file has been removed from the tree, but the commit is
# still in the history and the repository is public, so it is still readable.
# That is accepted rather than fought: the key was already exposed, making the
# repository private for a day did not recall it, and rewriting history would
# not either.
#
# What matters is that it can never sign again, and this list is keyed on the
# fingerprint rather than on a path for exactly that reason — deleting a key
# file does not make it unexposed. Replaced by the key pinned at
# firmware/certs/hk-signing-key.pub.bin, which was generated offline and has
# never been in the repository.
a568a512fe625e1c1ae07cc3d4498156936b56c842fb835aa5495c9fa178bad2
