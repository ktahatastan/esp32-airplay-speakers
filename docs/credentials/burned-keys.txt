# Signing keys that must never sign a release again, by SHA-256 of their
# public half (espsecure.py extract_public_key, then shasum -a 256).
#
# The publish job in .github/workflows/release.yml checks HK_SIGNING_KEY
# against this list and refuses to sign if it matches. A key lands here when it
# has been exposed, and it never leaves: "probably fine now" is not a state a
# signing key can be in.
#
# Blank lines and # comments are ignored.

# 2026-08-31 — the first key, generated and committed to the repository while
# it was public (commit 9afd991, pushed 15:33 UTC to a repo that was public at
# the time). The repository is private now and the key file has been removed,
# but it was publicly readable for a period and GitHub's public event stream is
# scraped within minutes. Replaced by the key pinned at
# firmware/certs/hk-signing-key.pub.bin, which was generated offline and has
# never been in the repository.
a568a512fe625e1c1ae07cc3d4498156936b56c842fb835aa5495c9fa178bad2
