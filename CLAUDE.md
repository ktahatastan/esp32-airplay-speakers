# Harman Kardom — Claude Code

@AGENTS.md
@docs/10-agentic-development/README.md

Use `AGENTS.md` as the canonical contract. Read the relevant ADR and domain plan before editing. Project subagents live in `.claude/agents/`; shared settings live in `.claude/settings.json`. Do not create or commit runtime team/task state.
