# Codex configuration

Codex reads root `AGENTS.md`, repo skills from `.agents/skills/`, project configuration from `config.toml` when the repository is trusted, and project custom agents from `agents/*.toml`.
