# Claude Code configuration

The supported project instruction entry point is root `CLAUDE.md`. Team-shared settings are in `settings.json`; local overrides belong in ignored `settings.local.json`. Project subagents are under `agents/`.
